{
  "name": "sprite-to-aseprite",
  "private": true,
  "type": "module",
  "scripts": {
    "prompt": "node scripts/make-prompt.mjs",
    "codex:task": "node scripts/run-codex-task.mjs",
    "auto-dev": "node scripts/auto-dev-cycle.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "vite build"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vite": "^5.0.0",
    "vitest": "^2.0.0",
    "yaml": "^2.5.0"
  }
}
