{
  "name": "sprite-to-aseprite",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prompt": "node scripts/make-prompt.mjs",
    "codex:task": "node scripts/run-codex-task.mjs",
    "auto-dev": "node scripts/auto-dev-cycle.mjs",
    "auto-dev:dry": "node scripts/auto-dev-cycle.mjs --dry-run",
    "auto-dev:all": "node scripts/auto-dev-cycle.mjs --all",
    "auto-dev:until-stop": "node scripts/auto-dev-cycle.mjs --until-stop",
    "auto-dev:generate-tasks": "node scripts/auto-dev-cycle.mjs --generate-tasks",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "vite build"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vite": "^5.0.0",
    "vitest": "^2.0.0",
    "yaml": "^2.5.0"
  }
}
