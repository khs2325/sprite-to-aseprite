id: "024"
title: Add manual usage guide
status: backlog
priority: high
goal: Document how to run and use each browser-local MVP conversion mode.
context: This task was generated deterministically because a product-completeness audit found missing product work in the repository.
scope:
  summary: Document how to run and use each browser-local MVP conversion mode.
  allowed_paths:
    - docs/**
  forbidden_paths: &a1
    - .github/workflows/**
    - .env
    - .env.*
    - dist/**
    - build/**
non_goals:
  - Do not implement unrelated roadmap features.
  - Do not upload user artwork or use external processing services.
  - Do not change dependencies, lockfiles, or GitHub workflows.
requirements:
  - Document npm run dev, file selection, each available MVP import mode, conversion, preview, and .aseprite download.
  - State that artwork stays in the browser and flat PNG sources cannot recover original layers.
acceptance_criteria:
  - Document how to run and use each browser-local MVP conversion mode.
  - Focused tests cover the behavior introduced by this task.
  - Typecheck, tests, and build pass.
verification:
  - npm run typecheck
  - npm run test
  - npm run build
auto_merge:
  allowed: true
  max_changed_files: 8
  forbidden_paths: *a1
