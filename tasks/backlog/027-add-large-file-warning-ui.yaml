id: "027"
title: Add large-file warning UI
status: backlog
task_origin: product-completion
priority: high
goal: Warn users that very large sprites or long sequences may exceed browser memory limits.
context: This task was generated deterministically because a product-completeness audit found missing product work in the repository.
scope:
  summary: Warn users that very large sprites or long sequences may exceed browser memory limits.
  allowed_paths:
    - index.html
    - src/index.ts
    - src/app/**
    - src/**/*.test.*
    - docs/**
  forbidden_paths: &a1
    - .github/workflows/**
    - .env
    - .env.*
    - dist/**
    - build/**
non_goals:
  - Do not implement unrelated roadmap features.
  - Do not upload user artwork or use external processing services.
  - Do not change dependencies, lockfiles, or GitHub workflows.
requirements:
  - Use plain TypeScript and DOM APIs.
  - Do not add React, Vue, Svelte, or other dependencies.
  - Keep files processed browser-locally.
  - Do not upload user artwork.
  - Reuse existing app, core, importer, and exporter helpers.
  - Do not duplicate conversion logic.
  - Add focused tests.
  - Use a clear non-blocking warning and do not inspect or upload files outside the browser.
acceptance_criteria:
  - Warn users that very large sprites or long sequences may exceed browser memory limits.
  - Focused tests cover the behavior introduced by this task.
  - Typecheck, tests, and build pass.
verification:
  - npm run typecheck
  - npm run test
  - npm run build
auto_merge:
  allowed: true
  max_changed_files: 8
  forbidden_paths: *a1
